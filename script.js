

/*
Display:
Neccessary functions:
    display:
    check if number to display is > 7 digits
        if so, make font size smaller
    display digit when pressed on screen or on keyboard
    When operator is pressed  
        save digits on display 
        highlight operator
        when next digit is pressed
            clear display
            clear highlight
            display new digits
        when sum-operator or new operator is pressed
            perform operation on display-digits and saved digits
            return result and show on display

neccessary variables:
    input 1
    input 2
            */   


